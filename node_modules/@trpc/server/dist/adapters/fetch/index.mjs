import { f as assertNotBrowser, r as resolveHTTPResponse } from '../../resolveHTTPResponse-fa4e77dd.mjs';
import '../../TRPCError-e99e7cbe.mjs';
import '../../codes-f164ac85.mjs';
import '../../index-47817162.mjs';
import '../../transformTRPCResponse-c8138d5f.mjs';

assertNotBrowser();
async function fetchRequestHandler(opts) {
    const createContext = async ()=>{
        return opts.createContext?.({
            req: opts.req
        });
    };
    const url = new URL(opts.req.url);
    const path = url.pathname.slice(opts.endpoint.length + 1);
    const req = {
        query: url.searchParams,
        method: opts.req.method,
        headers: Object.fromEntries(opts.req.headers),
        body: await opts.req.text()
    };
    const result = await resolveHTTPResponse({
        req,
        createContext,
        path,
        router: opts.router,
        batching: opts.batching,
        responseMeta: opts.responseMeta,
        onError (o) {
            opts?.onError?.({
                ...o,
                req: opts.req
            });
        }
    });
    const res = new Response(result.body, {
        status: result.status
    });
    for (const [key, value] of Object.entries(result.headers ?? {})){
        if (typeof value === 'undefined') {
            continue;
        }
        if (typeof value === 'string') {
            res.headers.set(key, value);
            continue;
        }
        for (const v of value){
            res.headers.append(key, v);
        }
    }
    return res;
}

export { fetchRequestHandler };
